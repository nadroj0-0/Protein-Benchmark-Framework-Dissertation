#!/usr/bin/env bash

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=_homology_cluster_common.sh
source "$SCRIPT_DIR/_homology_cluster_common.sh"

DIAGNOSTIC_PILOT=1
FIXTURE_MODE=0
unset ATTRITION_POLICY ATTRITION_OVERRIDE PILOT_APPROVAL PILOT_RUN_DIR
unset PILOT_COMPLETION_MARKER PILOT_ATTRITION_REPORT PILOT_TASK_CONTEXT
unset PILOT_MEASUREMENT_EVIDENCE
unset EXPECTED_ATTRITION_POLICY_SHA256 EXPECTED_PILOT_APPROVAL_SHA256
unset EXPECTED_PILOT_COMPLETION_MARKER_SHA256 EXPECTED_PILOT_ATTRITION_REPORT_SHA256
unset EXPECTED_PILOT_TASK_CONTEXT_SHA256 EXPECTED_PILOT_MEASUREMENT_EVIDENCE_SHA256
export DIAGNOSTIC_PILOT UNIPROT_SOURCE_SCOPE FRAMEWORK_REVISION FROZEN_INPUT_MANIFEST
export UNIREF90_FASTA UNIREF90_FASTA_SHA256 IDMAPPING IDMAPPING_SHA256
export GOA GOA_SHA256 GO_OBO GO_OBO_SHA256 RESULTS_ROOT EXPECTED_MMSEQS_VERSION
export MMSEQS_BIN SPLIT_POLICY TRAINING_POPULATION SEED MIN_COUNT NO_DOWNLOADS RUN_ID
export REQUESTED_SLOTS FIXTURE_MODE UNIPROT_RELEASE GOA_RELEASE ONTOLOGY_RELEASE
[[ -z "${UNIPROT_SPROT_SEQUENCES:-}" ]] || export UNIPROT_SPROT_SEQUENCES UNIPROT_SPROT_SEQUENCES_SHA256
[[ -z "${UNIPROT_TREMBL_SEQUENCES:-}" ]] || export UNIPROT_TREMBL_SEQUENCES UNIPROT_TREMBL_SEQUENCES_SHA256

validate_common_contract
export EXPECTED_FROZEN_INPUT_MANIFEST_SHA256
launch_array diagnostic-pilot 1
